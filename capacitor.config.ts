import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plano-eficiencia-ancho-de-banda',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
